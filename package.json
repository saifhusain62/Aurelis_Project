{
  "name": "aurelis-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix client"
  }
}
