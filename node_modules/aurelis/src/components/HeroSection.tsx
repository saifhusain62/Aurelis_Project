'use client';

import React from 'react';
import Link from 'next/link';
import Image from 'next/image';
import { motion } from 'framer-motion';
import { FiArrowRight, FiArrowUpRight } from 'react-icons/fi';

export default function HeroSection() {
  return (
    <section className="relative min-h-screen w-full flex flex-col justify-between pt-36 pb-12 px-6 lg:px-16 overflow-hidden bg-[#09130d]">
      {/* Background Hero Image with smooth fade & subtle zoom */}
      <motion.div
        initial={{ scale: 1.12, opacity: 0 }}
        animate={{ scale: 1.03, opacity: 1 }}
        transition={{ duration: 1.4, ease: [0.16, 1, 0.3, 1] }}
        className="absolute inset-0 z-0"
      >
        <Image
          src="/herobg.png"
          alt="Aurelis Golf Estate"
          fill
          priority
          unoptimized
          className="object-cover object-center"
        />
      </motion.div>

      {/* Main Content Area */}
      <div className="relative z-20 max-w-[1650px] mx-auto w-full my-auto flex flex-col lg:flex-row lg:items-end justify-between gap-12 pt-8">

        {/* Left Side: Typography & Main CTAs */}
        <div className="max-w-2xl flex flex-col gap-6 sm:gap-8">

          {/* Tag Badges */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.7, delay: 0.2 }}
            className="flex items-center gap-2"
          >
            {/* Small emblem circle */}
            <motion.div
              whileHover={{ rotate: 15, scale: 1.1 }}
              className="w-8 h-8 rounded-full bg-[#c6f135] p-1.5 flex items-center justify-center shadow-md cursor-pointer overflow-hidden"
            >
              <Image
                src="/logo.svg"
                alt="Aurelis Emblem"
                width={20}
                height={20}
                className="w-full h-full object-contain"
              />
            </motion.div>

            {/* EST. Pill Badge */}
            <div className="bg-[#c6f135] text-[#0f2619] px-4 py-1.5 rounded-full text-xs font-extrabold tracking-wider uppercase shadow-md">
              PRIVATE MEMBERS CLUB · EST. 1968
            </div>
          </motion.div>

          {/* Heading with staggered text reveal */}
          <h1 
            style={{ fontFamily: 'var(--font-serif)' }} 
            className="text-5xl sm:text-7xl lg:text-[5.4rem] leading-[0.98] tracking-tight text-white font-normal drop-shadow-lg"
          >
            <motion.span
              initial={{ opacity: 0, y: 35 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, delay: 0.35, ease: [0.16, 1, 0.3, 1] }}
              className="italic block text-[#f8faf8] font-normal"
            >
              A quieter way
            </motion.span>
            <motion.span
              initial={{ opacity: 0, y: 35 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, delay: 0.5, ease: [0.16, 1, 0.3, 1] }}
              className="block font-medium"
            >
              to play the game.
            </motion.span>
          </h1>

          {/* Subtitle */}
          <motion.p
            initial={{ opacity: 0, y: 25 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.65 }}
            className="text-slate-200 text-base sm:text-lg lg:text-xl font-normal max-w-lg leading-relaxed text-shadow-sm"
          >
            Championship golf, considered hospitality and a private community shaped around the game.
          </motion.p>

          {/* CTA & Course Meta Stats */}
          <motion.div
            initial={{ opacity: 0, y: 25 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.8 }}
            className="flex flex-col items-start gap-4 pt-2"
          >
            {/* White CTA Pill Button */}
            <Link
              href="/membership"
              className="inline-flex items-center justify-between gap-4 bg-white text-[#0f2619] font-semibold text-sm sm:text-base pl-6 pr-2 py-2 rounded-full shadow-2xl group w-fit cursor-pointer"
            >
              <span>Explore Membership</span>
              <div className="w-10 h-10 rounded-full bg-[#c6f135] text-[#0f2619] flex items-center justify-center shadow-sm">
                <FiArrowUpRight className="w-5 h-5 transition-transform duration-300 ease-out group-hover:rotate-45" />
              </div>
            </Link>

            {/* Course Meta Info */}
            <div className="flex items-center gap-2.5 text-slate-300 text-xs sm:text-sm font-medium tracking-wide pl-1">
              <span className="w-2 h-2 rounded-full bg-[#c6f135] " />
              <span>18 Holes · Par 72 · 7,164 Yards</span>
            </div>
          </motion.div>

        </div>

        {/* Right Side: Floating Event Glass Card */}
        <motion.div
          initial={{ opacity: 0, y: 50, scale: 0.92 }}
          animate={{ opacity: 1, y: 0, scale: 1 }}
          transition={{ duration: 0.9, delay: 0.9, ease: [0.16, 1, 0.3, 1] }}
          className="w-full lg:max-w-md lg:ml-auto"
        >
          <motion.div
            whileHover={{ y: -6 }}
            transition={{ duration: 0.3 }}
            className="glass-card glass-card-hover p-6 sm:p-8 rounded-[2rem] relative border border-white/20 shadow-sm backdrop-blur-[10px]"
          >
            {/* Card Date Badge */}
            <div className="bg-[#c6f135] text-[#0f2619] px-4 py-1.5 rounded-full text-xs sm:text-sm font-bold inline-block shadow-sm">
              Aug 24, 2026
            </div>

            {/* Event Title & Subtitle */}
            <div className="mt-8 mb-4">
              <h3 className="text-white font-semibold text-xl sm:text-2xl tracking-tight leading-snug">
                Summer Members <br /> Invitational
              </h3>
              <p className="text-slate-300 text-xs sm:text-sm font-medium mt-2">
                Members Tournament · 08:00
              </p>
            </div>

            {/* Floating Arrow Button */}
            <Link
              href="/events"
              className="absolute bottom-6 right-6 w-11 h-11 rounded-full border border-white/30 flex items-center justify-center text-white hover:bg-white hover:text-[#0f2619] transition-all shadow-md group"
              aria-label="View Event Details"
            >
              <FiArrowUpRight className="w-5 h-5 transition-transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5" />
            </Link>
          </motion.div>
        </motion.div>

      </div>
    </section>
  );
}
